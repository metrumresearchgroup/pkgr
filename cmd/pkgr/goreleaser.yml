# goreleaser.yml
# Build customization
project_name: pkgr

release:
  prerelease: true

build:
  binary: pkgr
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

# goreleaser.yml
brews:
  # Repository to push the tap to.
  -
    github:
      owner: metrumresearchgroup
      name: homebrew-tap

