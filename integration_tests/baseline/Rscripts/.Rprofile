# if test-library doesn't yet exist and you 
# launch, this will fall back to _not_ finding
# and not set any dir
# we might need to adjust this to just create an empty
# dir or something, but for now will just explode
stopifnot(dir.exists('../test-library'))
.libPaths('../test-library')
